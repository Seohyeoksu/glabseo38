streamlit>=1.34        # 웹 앱 프레임워크
openai>=1.14           # GPT-4o API 호출
pandas>=2.2            # 데이터프레임 처리
pypdf>=3.16            # PDF 텍스트 추출
Pillow>=10.2           # 이미지 핸들링
openpyxl>=3.1          # .xlsx 읽기
xlrd>=2.0              # .xls 읽기 (옵션이지만 호환 권장)

# ──────── 선택/편의 ────────
tiktoken               # 토큰 계산 (비용 예측)
python-dotenv          # .env 파일로 API 키 관리
aiohttp                # 이미지 다중 비동기 호출 시 사용
pdf2image              # 스캔 PDF OCR 필요 시
pytesseract            # ↑와 함께 OCR 사용 시
